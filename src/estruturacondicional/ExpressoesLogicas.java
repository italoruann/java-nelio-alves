package estruturacondicional;

public class ExpressoesLogicas {
    public static void main(String[] args) {
        // Operadores lógicos
        /*
            && -> E/
            || -> OU/
            ! -> NÂO/

            && -> Todas as condições precisam ser verdadeiras, se um der false, retorna false.
            || -> Todas as condições não precisam ser verdadeiras, basta apenas uma ser.
            ! -> Vai inverter o valor de uma condição. Por exemplo, se uma condição for verdadeira, se tornará falsa.

          */
    }
}
